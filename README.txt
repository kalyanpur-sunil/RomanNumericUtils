RomanNumericUtils
-------------------------------
RomanNumeralConvertor implements RomanNumeral interface.

The class RomanNumeralConvertor implements methods capable of converting a 
regular, decimal number into a Roman numeral, and another method that 
converts a Roman numeral to a decimal number.

Folder also includes documentation in the doc folder